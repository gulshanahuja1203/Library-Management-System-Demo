This folder contains all the main files that are included in the project.
The DAL is the Data Access Layer that has the models through which the data is being extracted from the backend which in our case is the SQL Server.
Models contains the View Models that holds the properties that are required for this project.
View contains all the views inside the Books folder.
